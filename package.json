{
  "name": "english",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@tanstack/react-query": "^5.62.7",
    "sass": "^1.49.9",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "deploy": "gh-pages -b build -d build -u \"github-actions-bot <support+actions@github.com>\"",
    "dev": "npm run dev -w @english/application",
    "build": "npm run build -w @english/application",
    "lint": "npx eslint .",
    "start": "npm run start -w @english/application",
    "prettier:fix": "npx prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "eslint-config-react-app": "^7.0.0",
    "gh-pages": "^3.2.3",
    "prettier": "3.2.5"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
